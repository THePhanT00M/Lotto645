/**
 * 등급 기준
 *
 * 관리자 판단은 서버(lib/auth/admin)에서 하지만, 화면에서도 같은 값을 보여
 * 줘야 한다. 그 파일은 서비스 롤 클라이언트를 함께 들여오므로 브라우저에서
 * 가져다 쓸 수 없어, 숫자만 여기에 둔다.
 */

/** 관리자 기능을 쓸 수 있는 최소 등급 */
export const ADMIN_LEVEL = 2
